struct maebot_imu_t
{
    int64_t utime;      // variscite

    double raw_acceleration[3];        // x, y, z
    double raw_angular_velocity[3];    // x, y, z  -- around z-axis is dTheta/dt
    double raw_orientation[3];         // x, y, z  // around z-axis is theta
}
